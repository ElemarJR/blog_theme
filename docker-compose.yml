# Development project infrastructure generated over the Docker. For see how up
# the infrastructure read the README file.

version: "2"

services:
  # PHP 7.1 with project required extensions.
  php:
    build:
      context: ./app/docker/php
    volumes:
      - .:/app:cached
      - ./.ssh:/root/.ssh
    depends_on:
      - db

  # Image for install and update project PHP packages.
  composer:
    image: composer
    volumes:
      - .:/app:cached
      # Map local user Composer directory with container for cache packages
      - ~/.docker_cache/composer:/root/composer

  # Database image to run the mysql
  db:
    image: mariadb:10.3
    ports:
        - "3306:3306"
    volumes:
       - ./database:/var/lib/mysql:cached
       - ./app/initdb:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: project
      MYSQL_USER: project
      MYSQL_PASSWORD: project

  # Run the applicatiion over HTTP on localhost:8080
  server:
    command: wp server --host=0.0.0.0
    build:
      context: ./app/docker/php
    ports:
      - "8080:8080"
    volumes:
      - .:/app:cached
    depends_on:
      - db
    environment:
      # Run $ sudo ifconfig en0 alias 10.254.254.254 255.255.255.0
      # to create a alias IP for the host machine and connect with Xdebug.
      # This command must be executed every time you restartr your machine.
      # Switch the value to 'On' to enable the remote connection.
      PHP_XDEBUG_REMOTE_ENABLE: "Off"

  # Node 8.2 with Grunt and Bower
  node:
    build:
      context: ./app/docker/node
    volumes:
      - .:/app:cached

  # Watch changes in the project and update the files local server
  watch:
    command: grunt watch
    build:
      context: ./app/docker/node
    ports:
      - "35729:35729"
    volumes:
      - .:/app:cached
